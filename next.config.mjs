/** @type {import('next').NextConfig} */
///const nextConfig = {};

///export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  pageExtensions: ['tsx', 'ts', 'jsx', 'js'],
};

export default nextConfig;

